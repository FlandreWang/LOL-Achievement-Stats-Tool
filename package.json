{
  "name": "lolachievements",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build",
    "dev:server": "pnpm --filter server dev",
    "dev:all": "pnpm --parallel dev"
  }
}
