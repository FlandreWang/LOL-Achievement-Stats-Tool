# 成就维度视图 - 开发计划

## 任务清单

- [ ] 1. 修改 `src/stores/record.js`，新增 `getAchProgress(achId, heroIds)` 方法，返回 `{ completed, total }`
- [ ] 2. 创建 `src/components/achievement/AchievementHeroCard.vue`，实现成就维度下的英雄卡片组件（头像、名称、完成状态标识、点击跳转）
- [ ] 3. 创建 `src/components/achievement/AchievementView.vue`，实现成就维度主视图（成就选择器 + 进度条 + 搜索框 + 英雄网格 + 排序 + 空状态）
- [ ] 4. 修改 `src/views/Home.vue`，添加视图切换控件（segmented control），根据 viewMode 条件渲染英雄维度或成就维度视图
