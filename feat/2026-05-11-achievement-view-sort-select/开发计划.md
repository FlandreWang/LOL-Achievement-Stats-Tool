# 成就维度页面排序与多选 - 开发计划

## 任务清单

### 排序功能

- [ ] 1. 修改 `AchievementView.vue`，添加排序状态（`achSort`）和排序选项定义，默认值 `incomplete-first`
- [ ] 2. 修改 `AchievementView.vue`，在搜索栏旁添加排序下拉 UI（复用 HeroSortSelect 样式模式）
- [ ] 3. 修改 `AchievementView.vue`，实现 `sortedHeroes` 计算属性，替换原有的硬编码排序逻辑
- [ ] 4. 修改 `AchievementView.vue`，将 `filteredHeroes` 改为基于 `sortedHeroes` 过滤

### 多选与批量操作

- [ ] 5. 修改 `AchievementView.vue`，添加多选状态（`selectedHeroes` Set、`isMultiSelectMode`）
- [ ] 6. 修改 `AchievementView.vue`，添加多选模式切换按钮和全选/取消全选按钮
- [ ] 7. 修改 `AchievementHeroCard.vue`，添加 `selectable` / `selected` props 和 checkbox UI
- [ ] 8. 修改 `HeroListCard.vue`，在成就维度模式下添加 `selectable` / `selected` props 和 checkbox UI
- [ ] 9. 修改 `AchievementView.vue`，将多选状态传递给子组件，处理选中事件
- [ ] 10. 修改 `stores/record.js`，添加 `batchToggle` 方法
- [ ] 11. 修改 `AchievementView.vue`，添加底部批量操作栏（批量完成/批量未完成）
- [ ] 12. 修改 `AchievementView.vue`，实现批量操作逻辑和完成后的状态清理
