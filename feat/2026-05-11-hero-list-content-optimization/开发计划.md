# 英雄列表内容优化 - 开发计划

## 任务清单

- [ ] 1. 修改 `packages/frontend/src/components/hero/HeroListCard.vue` 的 `<script setup>`，引入 `useRecordStore` 和 `useAchievementStore`，删除 `achievements` prop，新增 `heroAchievements`、`completedCount`、`progressPercent` computed
- [ ] 2. 修改 `HeroListCard.vue` 的缩略区模板，在英雄名称下方添加角色标签区域（从展开区移入）
- [ ] 3. 修改 `HeroListCard.vue` 的缩略区模板，在展开按钮左侧添加总体成就进度条
- [ ] 4. 修改 `HeroListCard.vue` 的展开区模板，移除角色标签，替换为逐项成就列表
- [ ] 5. 修改 `HeroListCard.vue` 的展开区 Transition，调整 max-h 限制以支持成就列表内容
