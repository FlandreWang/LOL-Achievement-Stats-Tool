# 英雄列表布局切换 - 开发计划

## 任务清单

### 阶段一：核心组件开发

- [ ] 1. 创建 `src/components/hero/HeroListCard.vue`，实现横向卡片组件基础结构
  - 左侧头像（64x64）
  - 右侧英雄名称、称号
  - 展开/折叠图标指示器
  - 点击展开/折叠功能
  - 折叠状态下的样式

- [ ] 2. 在 `src/components/hero/HeroListCard.vue` 中实现展开状态
  - 展开后显示英雄角色标签
  - 展开后显示成就完成进度条
  - 展开后显示各成就的完成状态列表
  - 展开/折叠动画效果（CSS Transition）

- [ ] 3. 创建 `src/components/hero/HeroList.vue`，实现列表布局容器
  - 接收 heroes 和 loading props
  - 渲染 HeroListCard 列表
  - 加载状态骨架屏
  - 空状态处理

### 阶段二：布局切换功能

- [ ] 4. 修改 `src/views/Home.vue`，添加布局切换按钮
  - 在搜索栏旁添加网格/列表切换按钮
  - 使用 Grid3X3 和 List 图标
  - 当前布局高亮显示

- [ ] 5. 在 `src/views/Home.vue` 中集成 HeroList 组件
  - 根据 layoutMode 条件渲染 HeroGrid 或 HeroList
  - 传递 heroes 和 loading 数据

- [ ] 6. 修改 `src/components/achievement/AchievementView.vue`，添加布局切换按钮
  - 在搜索栏旁添加网格/列表切换按钮
  - 与英雄tab保持一致的 UI 风格

- [ ] 7. 在 `src/components/achievement/AchievementView.vue` 中集成 HeroList 组件
  - 根据 layoutMode 条件渲染网格或列表
  - 传递英雄数据和成就完成状态

### 阶段三：偏好记忆与优化

- [ ] 8. 在 `src/views/Home.vue` 中实现布局偏好存储
  - 使用 localStorage 存储英雄tab布局偏好
  - onMounted 时读取偏好
  - watch layoutMode 时保存偏好

- [ ] 9. 在 `src/components/achievement/AchievementView.vue` 中实现布局偏好存储
  - 使用 localStorage 存储成就tab布局偏好
  - 独立于英雄tab的偏好

- [ ] 10. 优化 HeroListCard 的展开/折叠动画
  - 使用 max-height 动画实现平滑过渡
  - 添加 chevron 旋转动画

### 阶段四：测试与收尾

- [ ] 11. 测试英雄tab的布局切换功能
  - 网格视图正常显示
  - 列表视图正常显示
  - 切换流畅无闪烁

- [ ] 12. 测试成就tab的布局切换功能
  - 列表视图显示成就完成状态
  - 搜索功能正常工作
  - 成就进度正确显示

- [ ] 13. 测试移动端适配
  - 小屏幕下横向卡片正常显示
  - 展开内容合理换行
  - 触摸操作流畅

- [ ] 14. 测试布局偏好记忆
  - 刷新页面后布局保持
  - 英雄tab和成就tab偏好独立
